---
phase: 4
plan: 4.3
title: "Footer redesign (SVG icons, tagline, nav) + hero refinements (poster, copy)"
wave: 1
depends_on: []
files_modified:
  - next-app/components/Footer.tsx
  - next-app/app/page.tsx
requirements:
  - FOOT-01
  - FOOT-02
  - FOOT-03
  - HERO-01
  - HERO-02
autonomous: true
---

# Plan 4.3 — Footer Redesign + Hero Refinements

## Objective

Two surface improvements that appear on every page:

1. **Footer**: Add inline SVG LinkedIn icon, a mission tagline ("Advancing equity through undergraduate research"), and a nav row with links to About, Team, Publications, Events, and Connect.

2. **Hero (home page)**: Verify the poster image path is correct and loads instantly on slow connections; keep the current hero copy "Where Research Meets Reach" — it is distinctive and on-brand (HERO-02 decision: no change needed). Add `loading="eager"` hint via a preload link in the layout, and document poster path verification.

---

## Footer Tasks

<task id="4.3.1">
<title>Rebuild Footer with SVG icon, tagline, and nav links</title>

<read_first>
- next-app/components/Footer.tsx — full file
</read_first>

<action>
Replace the entire contents of `next-app/components/Footer.tsx` with the redesigned version below. Key changes:
- Three-row layout: tagline row, nav row, copyright + social row
- Inline SVG LinkedIn icon (matches connect page treatment)
- Mission tagline: "Advancing equity through undergraduate research"
- Nav links: About, Team, Publications, Events, Connect
- Uses Next.js `Link` for internal routes
- Maintains brand navy (#1f3a5f) background with white text

```tsx
"use client";

import Link from "next/link";

const navLinks = [
  { label: "About", href: "/about" },
  { label: "Team", href: "/team" },
  { label: "Publications", href: "/publications" },
  { label: "Events", href: "/events" },
  { label: "Connect", href: "/connect" },
];

export function Footer() {
  const year = new Date().getFullYear();

  return (
    <footer className="border-t border-[#e2e8f0] bg-[#1f3a5f]">
      <div className="w-[min(1120px,92vw)] mx-auto py-10 grid gap-6">
        {/* Tagline */}
        <div>
          <p className="text-white font-bold text-[1.1rem] m-0">
            REACH Equity Lab
          </p>
          <p className="text-white/70 text-[0.9rem] mt-1 m-0">
            Advancing equity through undergraduate research
          </p>
        </div>

        {/* Nav links */}
        <nav aria-label="Footer navigation">
          <ul className="flex flex-wrap gap-x-6 gap-y-2 list-none m-0 p-0">
            {navLinks.map((link) => (
              <li key={link.href}>
                <Link
                  href={link.href}
                  className="text-white/80 no-underline text-[0.9rem] hover:text-white transition-colors duration-150"
                >
                  {link.label}
                </Link>
              </li>
            ))}
          </ul>
        </nav>

        {/* Bottom row: copyright + social */}
        <div className="flex flex-wrap justify-between items-center gap-3 border-t border-white/10 pt-5">
          <p className="text-white/60 text-[0.85rem] m-0">
            &copy; {year} REACH Equity Lab. All rights reserved.
          </p>
          <a
            href="https://www.linkedin.com/company/reach-equity-lab/"
            target="_blank"
            rel="noreferrer"
            className="flex items-center gap-2 text-white/80 no-underline text-[0.9rem] font-medium hover:text-white transition-colors duration-150"
            aria-label="REACH Equity Lab on LinkedIn"
          >
            {/* LinkedIn SVG icon */}
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="20"
              height="20"
              viewBox="0 0 24 24"
              fill="currentColor"
              aria-hidden="true"
            >
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z" />
            </svg>
            LinkedIn
          </a>
        </div>
      </div>
    </footer>
  );
}
```
</action>

<acceptance_criteria>
- Footer renders tagline "Advancing equity through undergraduate research" below "REACH Equity Lab" label
- Nav row shows: About, Team, Publications, Events, Connect — all as Next.js `Link` components
- LinkedIn icon is an inline SVG (not an img tag or external icon library)
- LinkedIn link points to the real company URL `https://www.linkedin.com/company/reach-equity-lab/`
- Copyright line shows current year dynamically
- Footer appears correctly on all routes (it is in the layout)
- No TypeScript errors
</acceptance_criteria>
</task>

---

## Hero Tasks

<task id="4.3.2">
<title>Verify poster image path and add preload for slow connections</title>

<read_first>
- next-app/app/page.tsx — hero section (lines 11–22)
- next-app/app/layout.tsx — head/metadata section
</read_first>

<action>
**Verify poster path**: The `<video>` tag in `next-app/app/page.tsx` already has `poster="/reach-banner.png"`. Confirm the file exists at `next-app/public/reach-banner.png`. If it does, no change to the video tag is needed.

**Add preload link**: To ensure the poster image is requested early on slow connections, add a `<link rel="preload">` in the Next.js layout metadata. In `next-app/app/layout.tsx`, find or create the `<head>` section and add:

```tsx
// Inside the returned JSX, within <html>/<head>:
<link rel="preload" as="image" href="/reach-banner.png" />
```

If `layout.tsx` does not have an explicit `<head>` tag (relying on Next.js metadata API), add it as a `<head>` child element inside the `<html>` tag:

```tsx
<html lang="en">
  <head>
    <link rel="preload" as="image" href="/reach-banner.png" />
  </head>
  <body>
    {/* ...existing body content... */}
  </body>
</html>
```

**HERO-02 decision**: Current copy "Where Research Meets Reach" is kept as-is. It is distinctive, memorable, and on-brand. No copy change required.
</action>

<acceptance_criteria>
- `next-app/public/reach-banner.png` exists (verify with ls or Glob)
- `<link rel="preload" as="image" href="/reach-banner.png" />` is present in the rendered HTML `<head>`
- Video poster attribute still points to `/reach-banner.png`
- Hero copy "Where Research Meets Reach" is unchanged
- No TypeScript errors, `npm run build` exits 0
</acceptance_criteria>
</task>

<task id="4.3.3">
<title>Verify footer renders on all routes and hero loads correctly</title>

<read_first>
- next-app/components/Footer.tsx — final state
- next-app/app/layout.tsx — confirm Footer is imported and rendered
</read_first>

<action>
Manual verification checklist (no code change unless a gap is found):

**Footer:**
1. Run `npm run build` — zero errors
2. Open `/`, `/about`, `/team`, `/publications`, `/events`, `/connect` — footer appears on each
3. Footer shows: REACH Equity Lab label, tagline, nav links, copyright, LinkedIn icon
4. Click each nav link — routes correctly
5. Click LinkedIn icon — opens company page in new tab
6. Inspect on mobile (375px) — nav links wrap cleanly, copyright + social stack or wrap

**Hero:**
1. Open `/` in Chrome DevTools → Network tab → throttle to "Slow 3G"
2. Hard-refresh — poster image (`reach-banner.png`) appears immediately before video loads
3. Confirm no layout shift when video loads
4. Verify hero copy reads "Where Research Meets Reach"
</action>

<acceptance_criteria>
- `npm run build` exits 0
- Footer visible and correct on all routes
- Poster image renders before video loads on slow connections
- Hero copy unchanged
</acceptance_criteria>
</task>

---

## PLANNING COMPLETE
