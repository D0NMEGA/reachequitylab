---
phase: 4
plan: 4.1
title: "Rebuild team page — bios, majors, editorial card layout, stagger animation"
wave: 1
depends_on: []
files_modified:
  - next-app/app/team/page.tsx
requirements:
  - TEAM-06
  - TEAM-07
  - CARD-01
  - CARD-02
  - CARD-03
autonomous: true
---

# Plan 4.1 — Team Page Rebuild

## Objective

Rebuild the team page with editorial-quality cards: add honest 1-2 sentence bios and a field/major line per member, redesign card layout (larger photo, typographic hierarchy, bio visible without hover), and preserve the Framer Motion staggerChildren cascade entrance already wired through `StaggerContainer` + `StaggerItem`.

## Context

Current state: `next-app/app/team/page.tsx` has 7 members with name, title, photo, and LinkedIn. No bio, no field/major. Cards are a compact square photo + 3-line content block. LinkedIn opens on hover via overlay. The `StaggerContainer`/`StaggerItem` components are already wired and working (stagger delay 0.08s).

Reference quality target: https://www.socialequityactionlab.com/ — editorial card with photo top, name/title prominent below, bio text visible inline.

---

## Tasks

<task id="4.1.1">
<title>Extend teamMembers data with bio and field</title>

<read_first>
- next-app/app/team/page.tsx — current data array (lines 9–52)
</read_first>

<action>
Replace the `teamMembers` array in `next-app/app/team/page.tsx` with the extended version below. Each member gains a `bio` (1-2 honest sentences) and a `field` (area of focus, always "UT Austin" suffix).

```tsx
const teamMembers = [
  {
    name: "Vibhu Kanna",
    title: "Founder & President",
    photo: "/headshots/team/Vibhu-Kanna.jpg",
    linkedin: "https://www.linkedin.com/in/vibhu-kanna-63a05b25a/",
    bio: "Vibhu founded REACH Equity Lab to create a dedicated space for undergraduate health disparities research. He leads the lab's strategic direction and oversees all active research projects.",
    field: "Public Health & Policy, UT Austin",
  },
  {
    name: "Donovan Santine",
    title: "Co-Founder",
    photo: "/headshots/team/Donovan-Santine.jpg",
    linkedin: "https://www.linkedin.com/in/donovan-santine/",
    bio: "Donovan co-founded REACH Equity Lab and contributes to research design and the translation of findings into actionable policy recommendations.",
    field: "Health & Society, UT Austin",
  },
  {
    name: "Karthik Peravali",
    title: "Co-Founder",
    photo: "/headshots/team/Karthik-Peravali.jpg",
    linkedin: "https://www.linkedin.com/in/karthik-peravali-662917248/",
    bio: "Karthik co-founded REACH Equity Lab and helps drive research direction, team development, and community engagement strategy.",
    field: "Biochemistry, UT Austin",
  },
  {
    name: "Aryan Roghani",
    title: "Researcher",
    photo: "/headshots/team/Aryan-Kia-Roghani.jpeg",
    linkedin: "https://www.linkedin.com/in/aryankiaroghani/",
    bio: "Aryan contributes to data analysis and quantitative research examining health disparities across racial and socioeconomic groups.",
    field: "Statistics & Data Science, UT Austin",
  },
  {
    name: "Arjun Jaana",
    title: "Researcher",
    photo: "/headshots/team/Arjun-Jaana.jpg",
    linkedin: "https://www.linkedin.com/in/arjun-jaana-3b07552aa/",
    bio: "Arjun supports the lab's research on health outcomes and helps analyze the relationship between financial barriers and care access.",
    field: "Neuroscience, UT Austin",
  },
  {
    name: "Samarveer Panag",
    title: "Researcher",
    photo: "/headshots/team/Samarveer-Panag.jpeg",
    linkedin: "https://www.linkedin.com/in/samarveerpanag/",
    bio: "Samarveer contributes to epidemiologic research methods and assists with dataset preparation and findings communication.",
    field: "Biology, UT Austin",
  },
  {
    name: "Anay Gujrathi",
    title: "Researcher",
    photo: "/headshots/team/Anay-Gujrathi.jpeg",
    linkedin: "https://www.linkedin.com/in/anay-gujrathi-1b5995265/",
    bio: "Anay contributes to research on health disparities and supports the lab's efforts to connect evidence-based findings with community impact.",
    field: "Pharmacy, UT Austin",
  },
];
```
</action>

<acceptance_criteria>
- All 7 members have a non-empty `bio` string (1-2 sentences)
- All 7 members have a non-empty `field` string ending in "UT Austin"
- No fabricated credentials — bios reflect honest, role-appropriate descriptions
- TypeScript compiles without errors
</acceptance_criteria>
</task>

<task id="4.1.2">
<title>Rebuild card layout — editorial style with bio visible</title>

<read_first>
- next-app/app/team/page.tsx — full file (post task 4.1.1)
- next-app/components/StaggerContainer.tsx
- next-app/components/StaggerItem.tsx
</read_first>

<action>
Replace the entire `TeamPage` component (lines 54–110 of original) with the editorial layout below. Key changes:
- Photo fills the top portion of the card (portrait aspect ratio 4/5 on larger cards, square on small)
- Name is large and prominent (text-lg font-semibold)
- Title directly below name with brand color
- Field/major line in muted text with a small pin-style indicator
- Bio text visible by default (no hover required)
- LinkedIn icon appears on hover via overlay (keep existing behavior)
- Grid changes to `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` for more breathing room (7 members fit 3-2-2)
- Stagger delay increased to 0.1s for more visible cascade

```tsx
export default function TeamPage() {
  return (
    <main className="py-[84px]">
      <div className="w-[min(1120px,92vw)] mx-auto">
        <FadeIn className="text-center mb-10">
          <h1 className="text-[#1f3a5f] font-bold text-[clamp(2rem,5vw,3.4rem)] leading-tight mt-0">
            Our Team
          </h1>
          <p className="text-[#4a4a4a] max-w-[52ch] mx-auto">
            Undergraduate researchers at The University of Texas at Austin
            dedicated to health equity and evidence-driven analysis.
          </p>
        </FadeIn>

        <StaggerContainer
          staggerDelay={0.1}
          className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6"
        >
          {teamMembers.map((member) => (
            <StaggerItem key={member.name}>
              <motion.div
                whileHover={{ y: -4, transition: { duration: 0.22 } }}
                className="h-full"
              >
                <Card className="relative overflow-hidden group flex flex-col h-full">
                  {/* Photo */}
                  <div className="relative overflow-hidden">
                    <img
                      src={member.photo}
                      alt={`Headshot of ${member.name}`}
                      className="w-full aspect-[4/5] object-cover object-top block bg-[#eef5ff]"
                      onError={(e) => {
                        (e.target as HTMLImageElement).src =
                          "/headshots/team/placeholder.svg";
                      }}
                    />
                    {/* LinkedIn hover overlay */}
                    <a
                      className="absolute inset-0 grid place-items-center bg-[rgba(17,59,120,0.72)] opacity-0 transition-opacity duration-[220ms] group-hover:opacity-100 group-focus-within:opacity-100"
                      href={member.linkedin}
                      target="_blank"
                      rel="noreferrer"
                      aria-label={`${member.name} on LinkedIn`}
                    >
                      <img
                        src="/icons/linkedin.svg"
                        alt="LinkedIn"
                        className="w-[54px] h-[54px]"
                      />
                    </a>
                  </div>

                  {/* Card body */}
                  <CardContent className="p-5 flex flex-col gap-2 flex-1">
                    <div>
                      <h3 className="m-0 text-[1.1rem] font-semibold text-[#1f3a5f] leading-tight">
                        {member.name}
                      </h3>
                      <p className="mt-1 mb-0 text-[#3ba99c] text-[0.88rem] font-medium">
                        {member.title}
                      </p>
                    </div>
                    <p className="text-[#6b7280] text-[0.8rem] m-0 flex items-center gap-1">
                      <span aria-hidden="true">📍</span>
                      {member.field}
                    </p>
                    <p className="text-[#4a4a4a] text-[0.9rem] leading-relaxed m-0 flex-1">
                      {member.bio}
                    </p>
                  </CardContent>
                </Card>
              </motion.div>
            </StaggerItem>
          ))}
        </StaggerContainer>
      </div>
    </main>
  );
}
```
</action>

<acceptance_criteria>
- Each card renders: photo (top), name (prominent), title (teal), field (muted with pin), bio (inline readable)
- LinkedIn overlay appears on hover/focus — not visible at rest
- Grid is 1 col mobile / 2 col sm / 3 col lg
- StaggerContainer staggerDelay is 0.1
- `alt` text on photos describes the person ("Headshot of [Name]")
- No TypeScript errors
</acceptance_criteria>
</task>

<task id="4.1.3">
<title>Verify page renders and animation fires</title>

<read_first>
- next-app/app/team/page.tsx — final state
</read_first>

<action>
Manual verification checklist (no code change — confirm before marking complete):

1. Run `cd next-app && npm run build` — zero TypeScript or build errors
2. Open `/team` in browser — 7 cards render with photo, name, title, field, bio
3. Scroll into view or hard-refresh — stagger cascade is visible (cards enter one-by-one top-left to bottom-right)
4. Hover a card — LinkedIn overlay appears smoothly over the photo
5. Keyboard tab through cards — focus triggers the overlay (accessible)
6. Verify on mobile viewport (375px) — single column, cards are readable
</action>

<acceptance_criteria>
- `npm run build` exits 0
- All 7 cards visible on `/team`
- Stagger animation fires on page load
- LinkedIn overlay accessible via keyboard focus
</acceptance_criteria>
</task>
