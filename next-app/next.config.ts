import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static export not used — @cloudflare/next-on-pages handles deployment.
  // Edge runtime is set per-route where needed.
};

export default nextConfig;
